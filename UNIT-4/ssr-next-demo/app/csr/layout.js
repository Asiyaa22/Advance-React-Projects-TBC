export const metadata = {
  title: "CSR Products | SSR Next Demo",
  description: "Products rendered on the client after load.",
};

export default function CSRLayout({ children }) {
  return <>{children}</>;
}
